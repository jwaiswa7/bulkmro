const companyReport = () => {
    // $('.datatable').on('draw.dt', function (event) {
        // $.each($('.group.group-start'), function(){
        //     let $this = $(this).find('a')
        //     let accountTarget = $this.data('account-target');
        //     $this.append(' => '+accountTarget);
        // })
    // });
};

export default companyReport