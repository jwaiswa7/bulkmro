const ckeditor = () => {
    // $R('.htmlize', {
    //     removeScript: false,
    // });

    // ClassicEditor.create(document.querySelector('.htmlize'), { fullPage: true })
    //     .then(editor => {
    //         console.log(editor);
    //     })
    //     .catch(error => {
    //         console.error(error);
    //     });
};

export default ckeditor