<%= provide :container_fluid, true %>
<% provide :can_be_directly_created, true %>
<% provide :can_be_ajaxed, true %>

<% content_for :header_row do %>
  <th class="no-sort" width="98px">Quick Actions</th>
  <th data-name="name" class="text-left">Name</th>
  <th class="no-sort desktop text-left">SAP Status</th>
  <th class="no-sort desktop" data-name="is_active">Active</th>
  <th class="desktop text-left" data-name="created_at">Created</th>
<% end %>

<div class="col-lg-12">
  <%= render layout: 'shared/layouts/datatable' do %>
  <% end %>
</div>