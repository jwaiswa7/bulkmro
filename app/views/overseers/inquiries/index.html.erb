<%= provide :container_fluid, true %>
<% provide :can_be_ajaxed, true %>

<% content_for :header_row do %>
  <th class="no-sort"></th>
  <th class="no-sort">Status</th>
  <th class="no-sort" data-width="15%">Company</th>
  <th class="no-sort">Contact</th>
  <th data-width="30%" data-name="subject">Subject</th>
  <th class="no-sort">Inside Sales</th>
  <th class="no-sort">Outside Sales</th>
  <th data-name="created_at">Created</th>
<% end %>

<div class="col-lg-12">
  <%= render layout: 'shared/layouts/datatable' do %>
  <% end %>
</div>