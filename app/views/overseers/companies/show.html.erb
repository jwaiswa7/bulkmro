
<h4>Addresses</h4>

<a href="<%= new_overseers_company_address_path(@company) %>">
  <button type="button" class="btn btn-primary m-t-10">+ Add Address</button>
</a>

<div class="table-responsive">
  <table id="datatable-buttons" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <% @company.addresses.each do |address| %>
      <tr>
        <td><%= address.id %></td>
        <td><%= address.to_s %></td>
        <td><a href="<%= edit_overseers_company_address_path(address.company, address) %>">Edit</a></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

<hr />

<h4>Inquiries</h4>

<a href="<%= new_overseers_company_inquiry_path(@company) %>">
  <button type="button" class="btn btn-primary m-t-10">+ Add Inquiry</button>
</a>

<div class="table-responsive">
  <table id="datatable-buttons" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
      <th>Point of contact</th>
      <th>Actions</th>
    </tr>
    </thead>
    <tbody>
    <% @company.inquiries.each do |inquiry| %>
      <tr>
        <td><%= inquiry.id %></td>
        <td><%= inquiry.to_s %></td>
        <td><%= inquiry.contact.to_s %></td>
        <td><a href="<%= edit_overseers_company_inquiry_path(inquiry.company, inquiry) %>">Edit</a></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>

<h4>Brands</h4>

<div class="table-responsive">
  <table id="datatable-buttons" class="table table-striped table-bordered" cellspacing="0" width="100%">
    <thead>
    <tr>
      <th>Id</th>
      <th>Name</th>
    </tr>
    </thead>
    <tbody>
    <% @company.brands.each do |brand| %>
      <tr>
        <td><%= brand.id %></td>
        <td><%= brand.to_s %></td>
      </tr>
    <% end %>
    </tbody>
  </table>
</div>