<div class="col-lg-12">
  <div class="page-header">
    <h2> Tax Codes</h2>
  </div>

  <%= simple_form_for([:overseers, @tax_codes], :html => {:class => 'needs-validation', :'novalidate' => '', :'data-parsley-validate' => ''}) do |f| %>
    <%= f.input :code %>
    <%= f.input :tax_percentage %>
    <%= button_tag(type: "submit", class: "btn btn-warning btn-block") do %>
      <i class="fal fa-pen"></i> <%= submit_text(@tax_codes) %>
    <% end %>
  <% end %>
</div>