<%= render layout: 'customers/reports/tabs_layout' do %>
  <div class="page-header">
    <h2>Unique Skus</h2>
  </div>

  <div class="card">
    <div class="card-body">
      <%= bar_chart @chart[:data], @chart[:options].merge({:height => '180px', :id => 'unique_skus_data'}) %>
    </div>
  </div>
<% end %>